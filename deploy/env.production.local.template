# Muhtasham Admin (Next.js) production env template for DigitalOcean Droplet
# Copy to `.env.production.local` on the server and fill values.
# IMPORTANT: never commit real secrets to git.

# --- Admin panel login (required) ---
ADMIN_LOGIN=adminmuhtasham

# Generate these from the admin password (example: "admin123"):
#   node scripts/gen-admin-hash.js admin123
ADMIN_PASSWORD_SALT=
ADMIN_PASSWORD_HASH=

# Used to sign admin session cookie (required)
# Generate, for example:
#   node -e "console.log(require('crypto').randomBytes(48).toString('base64'))"
ADMIN_SESSION_SECRET=

# --- Database (required) ---
# Use an absolute path and ensure the process has write permissions.
# Example: /root/muhtashamadmin/data.sqlite
DATABASE_URL=

# --- Client (mobile app) JWT (required if mobile uses login/register) ---
# Generate a long random string (different from ADMIN_SESSION_SECRET).
CLIENT_JWT_SECRET=

# --- Uploads (optional) ---
# Directory for uploaded files. If empty, the app uses `public/uploads`.
#UPLOAD_DIR=

# --- Poster / 1C / SMS (optional; only needed if you use these features) ---
#POSTER_TOKEN=
#POSTER_ACCOUNT_ID=
#ONEC_BASE_URL=
#ONEC_LOGIN=
#ONEC_PASSWORD=
#ONEC_TOKEN=
#ONEC_CA_CERT_PATH=
#SMS_API_KEY=
#SMS_SENDER=

